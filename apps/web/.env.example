# ClickUp OAuth 2.0 Credentials (Optional)
# Only required if using the OAuth browser login flow.
# Users can also connect directly in the app using their ClickUp Personal API Key without these credentials.
VITE_CLICKUP_CLIENT_ID=
VITE_CLICKUP_CLIENT_SECRET=
VITE_CLICKUP_REDIRECT_URI=http://localhost:3456/callback
